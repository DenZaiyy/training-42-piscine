#include <stdio.h>
#include <unistd.h>

void ft_rev_int_tab(int *tab, int size)
{
}

int main()
{
    int tab;
    int size;

    tab = 2;
    size = 10;

    ft_rev_int_tab(tab, size);
    return 0;
}